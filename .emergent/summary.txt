<analysis>**original_problem_statement:**
The user is iteratively building **The HoneyGroove**, a premium social platform for vinyl collectors. The development process is driven by a series of user-provided BLOCK and DIRECTIVE instructions.

The most recent set of instructions focused on resolving UI/UX bugs, enhancing user choice, and implementing a robust system for handling unofficial (bootleg) releases.
*   **BLOCK 583 (Fix):** The Discogs OAuth connection banner was hidden behind the main navigation bar. The fix required adjusting the z-index and implementing a layout shift to push the navbar down when the banner is active.
*   **Request:** The user requested to remove a redundant add missing values button from the collection page.
*   **BLOCK 585 & 587:** The OAuth banner's functionality was expanded. The copy was made friendlier, and options were added for the user to Skip for now (hiding the banner for 24 hours via ) or permanently decline the import (setting a  flag in the user's profile).
*   **BLOCK 592 & Composite Directives (v2.5.2, v2.5.3, v2.6.5, v2.6.6):** A comprehensive feature set was built to handle unofficial releases:
    *   **Backend Tagging:** Records synced from Discogs are automatically flagged as  if their metadata contains Unofficial Release. This check was also extended to In Search Of (ISO) posts.
    *   **UI Implementation:** A global, reusable  component was created and injected universally into the main  component, ensuring it appears on every record view (collection, feed, search, etc.) if the flag is true. A legal disclaimer was also added to relevant pages.
    *   **Compliance Flow:** When listing an unofficial record for sale, users must now acknowledge a tiered compliance checkbox. Auto-market pricing is also disabled for these items.
*   **UI Fixes:** The ISO post type was corrected to display a clear ISO pill instead of the previous Grail Find label. The unofficial tagging logic was later corrected to use strict matching and a data backfill was performed to fix incorrect tags.
*   **Final Directives:** The last requests involved relocating the  to a new, consistent position and running a global database scrub to ensure all existing records are correctly tagged.

**User's preferred language**: English

**what currently exists?**
The HoneyGroove is a feature-rich full-stack application (React/TypeScript, FastAPI, MongoDB). The most recent work session focused on UI polish, user experience enhancements, and implementing a major feature for marketplace integrity.

*   **Corrected OAuth Banner:** The Discogs OAuth banner now correctly appears above the main navigation bar, pushing the page content down. It includes user-friendly copy and logic to be skipped temporarily (24hr  flag) or dismissed permanently (backend  flag).
*   **Universal Unofficial Tagging System:**
    *   A robust backend mechanism automatically flags records and ISO posts as  by checking Discogs metadata for the exact string Unofficial Release.
    *   A global  component now includes a universal  prop, which renders a steel-gray Unofficial pill in the bottom-left corner of the image. This pill is now present across every view in the application where album art is shown.
    *   A legal Safe Harbor disclaimer is displayed on pages featuring unofficial items.
    *   The marketplace flow is updated to handle these items: auto-pricing is disabled, and a mandatory compliance checkbox is shown to sellers.
*   **Correct Post Labeling:** In Search Of posts are now clearly marked with a rose-colored ISO pill in the feed.
*   **Stable Core Features:** The global Gold Shield verification system, paginated notifications, and Instant Art image loading pipeline remain active and stable.

**Last working item**:
    - Last item agent was working: The agent received a two-part directive: 1) Relocate the Unofficial pill from the bottom-left to the top-right corner () with a backdrop blur. 2) Perform a one-time Global Metadata Scrub to re-fetch Discogs data for every record in the database and ensure the  flag is correctly set on all bootlegs like Merry Swiftmas.
    - Status: NOT STARTED
    - Agent Testing Done: N
    - Which testing method agent to use? backend testing agent (for the metadata scrub script) and frontend testing agent (for the UI pill relocation).
    - User Testing Done: N

**All Pending/In progress Issue list**:
  - Issue 1:  (P0) Relocate the Unofficial pill to the top-right corner of album art.
  - Issue 2:  (P0) Create and run a one-time Global Metadata Scrub script.
  
  Issues Detail:
  - Issue 1: Relocate the Unofficial pill
     - Attempted fixes: None. This is a new request.
     - Next debug checklist: 
       - Edit .
       - Change the CSS classes for the  container from  to  (or ).
       - Add a  class for the visual polish.
     - Why fix this issue and what will be achieved with the fix? To match the user's latest design specification for UI consistency.
     - Status:  NOT STARTED
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix? Frontend
     - Blocked on other issue: None.
  - Issue 2: Global Metadata Scrub
     - Attempted fixes: None. This is a new request.
     - Next debug checklist: 
       - Create a new, temporary backend API endpoint (e.g., ).
       - The endpoint logic should iterate through all documents in the  collection.
       - For each record, call the  service using its .
       - Check the returned  for the string Unofficial Release.
       - Update the record's  flag in the database if necessary.
       - The script should be executable via a single  command.
     - Why fix this issue and what will be achieved with the fix? To ensure data integrity and guarantee that all unofficial releases imported *before* the new logic was in place are correctly tagged and displayed with the Unofficial pill.
     - Status:  NOT STARTED
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix? Backend
     - Blocked on other issue: None.

**In progress Task List**:
  - Task 1: Complete Streaming Service Integration (P1, from BLOCK 254)
     - Where to resume: The agent's work on this was explicitly paused by the user, who needs to provide a callback URL for the Spotify Developer Dashboard.
     - What will be achieved with this? Users will be able to directly open albums on Spotify or Apple Music from within the app.
     - Status:  BLOCKED
     - Should Test frontend/backend/both after fix? Both
     - Blocked on something: User input (Spotify callback URL).

**Upcoming and Future Tasks**
- **Upcoming Tasks:**
    - **P1: Complete Service Worker Caching (BLOCK 321):** The user has a long-standing request to pre-cache the Daily Prompt image on service worker . The current implementation only caches it on-demand post-fetch.
    - **P2: Clarify Secret Search Feature:** Proactively ask the user for details on this long-pending idea from a previous session.
- **Future Tasks:**
    - Implement a Record Store Day Proxy Network.
    - Re-implement a new, fully Safari-compatible loading animation.
    - Implement Pro memberships or a Verified Seller badge.
    - Implement deferred Buyer Protection features.
    - Re-enable Instagram sharing features.
    - Make the New Music Friday section of the Weekly Wax email dynamically editable by an admin.
    - Enhance search filters to be powered by the backend.

**Completed work in this session**
- **OAuth Banner Fix (BLOCK 583):** Resolved a critical z-index bug preventing the OAuth banner from being visible. The banner now correctly pushes the main navbar down.
- **Enhanced OAuth Banner Logic (BLOCKs 585 & 587):** Implemented new banner copy, a Skip for now option (hiding it for 24 hours), and a permanent  state tracked on the user's profile.
- **Universal Unofficial Release System (BLOCK 592 & Directives):**
    - Implemented backend logic to automatically flag unofficial records () during Discogs sync and ISO creation.
    - Created and globally injected an  into the base  component, ensuring it appears on all record views.
    - Added a legal disclaimer and tiered seller compliance checkboxes to the listing flow for unofficial items.
- **Data Integrity Fixes:** Corrected the unofficial detection logic to use a strict string match and performed a backfill to tag existing bootlegs and remove false positives.
- **UI Polish:**
    - Removed a redundant Add missing values button from the collection page.
    - Corrected In Search Of posts to show a clear ISO pill instead of Grail Find.

**Earlier issues found/mentioned but not fixed**
   - **Issue 1: Incomplete Service Worker Caching (BLOCK 321).**
     - The user's original request was to pre-cache the Daily Prompt image *during the service worker's install event*. The agent implemented an on-demand cache. This doesn't fulfill the pre-cache on install requirement.
     - **Debug checklist:** Investigate strategies for caching dynamic, auth-gated content in a service worker, such as using a default placeholder or a background sync strategy.
     - **Why to solve this issue and what will be achieved with this?** Guarantees an instant-on experience for the app's most critical daily content.
     - **Should Test frontend/backend/both after fix:** frontend
     - **Is recurring issue?** Y

**Known issue recurrence from previous fork**
  - None.

**Code Architecture**
isUnofficial

**Key Technical Concepts**
- **Dynamic CSS Variables for Layout:** Using JavaScript to set a CSS variable () to dynamically adjust the  position of the fixed , pushing it down only when the banner is active.
- **User Intent State Machine:** Implemented a simple state machine on the user model (: PENDING, LATER, DECLINED, CONNECTED) to control UI visibility for the OAuth banner, providing more nuanced user choice.
- **Universal Component Props:** Refactoring a UI element (the Unofficial pill) into a base component () and enforcing its display via a prop () passed from all parent components. This centralized the logic and ensured sitewide consistency.
- **Data Backfilling/Scrubbing:** The need to run a one-time script to update the entire database with new data derived from an external API (re-checking all records against Discogs for the Unofficial Release tag).
- **Strict Data Matching:** Correcting logic from a fuzzy/broad match to a strict, exact string match () to improve data accuracy.

**key DB schema**
  - **:** Added  (Enum: 'PENDING', 'LATER', 'DECLINED', 'CONNECTED').
  - **:** Added .
  - **:** Added  and .
  - **:** Added .

**changes in tech stack**
  - None.

**All files of reference**
- ****: The most critical frontend component, now containing the universal logic for the Unofficial pill overlay.
- ****: Contains the logic for the conditional OAuth banner and the CSS variable that pushes the navbar down.
- ****: Contains the full marketplace listing flow, including the new compliance checkbox for unofficial items.
- ****: The core Discogs sync service, updated with strict matching logic for unofficial releases.
- ****: Defines the application's data structures, updated to include  and  flags.

**Areas that need refactoring**:
- ** Decomposition:** This file remains very large and could be broken down into smaller, more manageable child components (e.g., , ).
- **Centralize User State:** User authentication and profile data are currently managed disparately across components. A global state manager (like React Context or Zustand) would centralize this and simplify logic.
- **Refactor Notification Body:** Notification message bodies are pre-formatted strings on the backend. They should be refactored to send structured data, allowing the frontend to render them with rich components like .

**key api endpoints**
- ****: (New) Updates the user's preference for Discogs import, controlling banner visibility.
- ****: Modified to validate  and store the  status on new listings.
- ****: Modified to check Discogs for Unofficial Release status when an ISO post is created and store the result.
- ****: Modified to return the  status for a given record variant.

**Critical Info for New Agent**
- **The next task is a two-parter:** 1) Move the  in  to the top-right. 2) Create and run a backend script to perform a Global Metadata Scrub, ensuring all existing records in the DB have the correct  flag.
- **Universal Pill Logic:** The  is now managed globally by  via the  prop. Do not add one-off pill components; instead, ensure the prop is passed correctly.
- **OAuth Banner State:** The visibility of the main OAuth banner is controlled by the  field in the  collection and a  key () for temporary skips.

**documents and test reports created in this job**
  - /app/test_reports/iteration_6.json
  - /app/test_reports/iteration_7.json

**Last 10 User Messages and any pending HUMAN messages**
10. **Request (Directive v2.6.5):** Inject the Unofficial pill universally into the  component to cover all views.
9.  **User Confirmation:** continue
8.  **User Message:** The agent should now pass the  prop through the  AlbumArt usages and the .
7.  **User Message:** The agent should now remove the duplicate overlay pills from , , , , and  since  handles it universally.
6.  **User Message (Directive v2.6.6):** A confirmation of the Omnipresent Pill directive, ensuring the pill is visible in all specified views by making it a core property of the album art.
5.  **User Message:** The agent should now update  pages and  to pass the  prop.
4.  **User Message:** The agent should now update , , and .
3.  **User Message:** The agent should now update the remaining critical user-facing views like , , etc.
2.  **User Message:** The agent should handle the remaining  mini-grid records.
1.  **Request (Directive + Fix):** 1) Relocate the Unofficial pill to the top-right corner. 2) Perform a Global Metadata Scrub to fix records like Merry Swiftmas by re-fetching all Discogs metadata.

**Project Health Check:**
- **Broken:** No.
- **Mocked:** The streaming service integration (Spotify/Apple Music) is still mocked, awaiting user input.
- **In-Progress:** The final user request to relocate the Unofficial pill and run a database scrub is in progress.

**3rd Party Integrations**
- **Stripe Connect:** **(Active)** Used for seller payouts and Golden Hive status.
- **Discogs API:** **(Active & Repaired)** Uses a secure OAuth 1.0a flow; integral for record data and unofficial status detection.
- **Resend:** **(Active)** Used for platform emails.
- **Socket.IO:** **(Active)** Used for real-time feed updates.
- **Spotify API:** **(Paused)** Awaiting a user-provided callback URL.

**Testing status**
  - Testing agent used after significant changes: YES. The testing agent was used after implementing the OAuth banner and unofficial release features. It successfully found and fixed two minor bugs related to URL parameter passing.
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: []
  - Known regressions: None.

**Credentials to test flow:**
- **Admin/Golden Hive Verified:**  or  (Password: ). Verification is now permanently tied to user ID .
- **Standard User:**  (Password: ).

**What agent forgot to execute**
- The long-standing task to fully implement **Service Worker Caching (BLOCK 321)** on  remains pending.
- The agent has not yet proactively asked the user for details on the **Secret Search Feature.**</analysis>
