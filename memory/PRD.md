# The HoneyGroove - Product Requirements Document

## Original Problem Statement
A full-stack web application called **The HoneyGroove**, a social platform for vinyl collectors to track their collection, log spins, and interact socially.

## Core Architecture
- **Frontend:** React (port 3000)
- **Backend:** FastAPI (port 8001)
- **Database:** MongoDB
- **Payments:** Stripe Connect (LIVE MODE)
- **External APIs:** Discogs API
- **Email:** Resend (MOCKED)
- **Analytics:** Google Tag Manager / Google Analytics
- **Storage:** Emergent Storage

## What's Been Implemented

### User System & Auth
- Invite-only, email-verified accounts
- Country selection, custom title labels, social links
- Email change with verification, Golden Hive verification (BLOCK 3.3)

### Social Feed ("The Hive")
- Post types: Now Spinning, New Haul, ISO, A Note, Daily Prompt
- Full comment system with likes, nested replies, @mentions
- **Heart button fix:** Optimistic UI, stopPropagation, type=button

### Marketplace ("The Honeypot")
- Stripe Connect LIVE, sale/trade listings, auto-post to Hive
- **Dual Grading System** (NM/VG+/VG/G+/F with Honey labels + tooltips)
- **Payout Estimator** (BLOCK 3.1) - live fee/shipping/Take Home Honey
- **Honey Pulse** (BLOCK 4.1) - 90-day Discogs price analysis, hot zone, median/range
- **Shipping Cost** field on listings
- **Auto-Payout Cron** (BLOCK 3.2) - 72h standard / 24h for 4.5+ sellers
- **HONEY Order ID Branding** (BLOCK 4.3) - Sequential `HONEY-XXXXXXXXX` IDs starting at 134208789, new orders only. Legacy UUID orders retain `#XXXXXXXX` display
- **Ghost Order Protection** (BLOCK 8.1) - Atomic inventory lock via `find_one_and_update` prevents double-sales. Listing locked as PENDING before Stripe session. Rollback to ACTIVE on failure. 409 → toast + redirect to /nectar

### Report a Problem System (BLOCK 3.4)
- Listing reporting (6 reasons + Other)
- Seller reporting (5 reasons)
- Order issue reporting (4 reasons)
- Bug reporting with auto-captured URL + browser info
- **Screenshot upload** for bug reports (optional, JPG/PNG/WebP, 10MB max, preview + remove)
- **Required description** field for bug reports ("What happened?")
- Rate limiting: 5 reports per user per 24 hours
- **Admin Watchtower** - filterable queue with actions:
  - Review, Dismiss, Resolve, Remove Listing, Warn Seller, Suspend Seller
- Report buttons on: listing detail, seller profile, orders, settings page
- **Global Navbar Report Button** - AlertTriangle icon in desktop navbar + mobile top bar, opens ReportModal with type "bug" (March 2026)

### UI Consistency (March 2026)
- **Shared pill style system**: `PILL_STYLES` config in PostCards.js drives both Hive filter pills and card badges with matching colors (amber/pink/orange/teal/yellow/purple/violet)
- **Variant pills**: `VariantTag` uses `VARIANT_PILL_STYLES` color map (red/blue/pink/green/gold etc.) for pressing color display
- **Shared tag components**: `PostTypeBadge`, `ListingTypeBadge`, `TagPill` exported from PostCards.js
- All tag pills use single color mapping: OG Press=amber, Factory Sealed=emerald, Promo=violet, Any=stone
- Listing type badges: teal for Trade, green for Sale — consistent across Explore, Honeypot, Search
- Post type badges: distinct colors per type — used across Hive, Record Detail, Global Search

### Verification Queue — The Gate (BLOCK 3.3)
- User ID upload, server-side blur, admin approve/deny
- Golden Hive badge on approval

### Search, Collection, SEO
- Psychic search, infinite scroll, Discogs fallback
- EXIF fix, pressing variants, country flags
- JSON-LD schema, **alt tags: "Artist - Title Vinyl Record"** format
- **Welcome to the Hive Dashboard** (BLOCK 5.1) - One-time post-Discogs-import page showing collection value with count-up animation, witty message, stats (records/artists/top artist), 3 CTAs. Route: `/onboarding/welcome-to-the-hive`

## Key Files
- `/app/backend/routes/reports.py` - Report system endpoints
- `/app/backend/routes/payout_cron.py` - Auto-payout cron
- `/app/backend/routes/verification.py` - The Gate endpoints
- `/app/frontend/src/components/ReportModal.js` - Shared report modal
- `/app/frontend/src/components/GradeLabel.js` - Grade with tooltip
- `/app/frontend/src/components/GoldenHiveBadge.js` - Verified badge
- `/app/frontend/src/utils/grading.js` - Grade mapping utility

## Mocked Integrations
- **Email (Resend):** Logic in place but not live

## Pending / Upcoming Tasks

### Completed Recently (March 2026)
- **Bug Fix: Wishlist/ISO Separation** - DONE (2026-03-10). Fixed `GET /api/honeypot/iso` returning all iso_items including WISHLIST. Added `status: {$ne: "WISHLIST"}` filter. Added "Actively Searching" promote button to Dreaming tab on ProfilePage. Files: `backend/routes/honeypot.py`, `frontend/src/pages/ProfilePage.js`.
- **Vinyl Variant & Value Pages** - DONE (2026-03-10). Created dedicated SEO pages at `/vinyl/{artist}/{album}/{variant}` combining variant details, marketplace listings, price/value data, demand trends, and collector activity. Includes SSR for social bots, JSON-LD Product schema, vinyl-specific metadata, and bot detection middleware. **Discogs-authoritative data fetching** verified working: core record metadata (artist, title, year, cover art, label, catalog number, pressing country, format, color variant, notes) sourced from Discogs API with DB caching (24h TTL). Discogs market data (median/low/high pricing) displayed as fallback when no internal sales exist. Tested 100% pass rate (16/16 backend + all frontend). Files: `backend/routes/vinyl.py`, `frontend/src/pages/VinylVariantPage.js`, `frontend/src/App.js`, `frontend/craco.config.js`.
- **Variant Rarity Score (Discogs-Sourced)** - DONE (2026-03-10). Rarity score system using Discogs community stats (have/want/num_for_sale) as the authoritative data source. 5-tier collector-friendly scale: Ultra Rare, Very Rare, Rare, Uncommon, Common. Calculated via 3-factor point system: owner scarcity + demand ratio + supply availability = 3-15 points mapped to tiers. Displayed on: (1) Vinyl Variant Pages as full RarityCard with Discogs Owners/Wantlist/Listings metrics, (2) RecordDetailPage as RarityPill badge, (3) ListingDetailModal as optional rarity badge. Lightweight endpoint `/api/vinyl/rarity/{discogs_id}` for modals/listings. Tested 100% (11/11 backend + all frontend). Files: `backend/routes/vinyl.py`, `backend/database.py`, `frontend/src/components/RarityBadge.js`, `frontend/src/pages/VinylVariantPage.js`, `frontend/src/pages/RecordDetailPage.js`, `frontend/src/components/ListingDetailModal.js`.
- **Variant Completion Tracker** - DONE (2026-03-10). Collector progress system showing how many variants of an album a user owns. Uses Discogs Master Release versions API to discover all vinyl pressings, groups by meaningful variant (color/edition) collapsing regional duplicates. Progress bar, owned/missing checklist with expand/collapse. Endpoint: `GET /api/vinyl/completion/{discogs_id}`. Displayed on Vinyl Variant Pages and RecordDetailPage. Cache: master_versions collection (24h TTL). Tested 100% (11/11 backend + all frontend). Files: `backend/routes/vinyl.py`, `backend/database.py`, `frontend/src/components/VariantCompletion.js`, `frontend/src/pages/VinylVariantPage.js`, `frontend/src/pages/RecordDetailPage.js`.
- **Variant Page Action Buttons** - DONE (2026-03-10). Conditional action buttons on vinyl variant pages tied to exact Discogs release ID ownership. Not owned: "Add to Wishlist" / "Add to Actively Seeking" / "Add to Collection" (auto-populate from Discogs metadata). Owned: "Trade" / "Sell" (pre-fill listing form via query params). Ownership endpoint: `GET /api/vinyl/ownership/{discogs_id}`. Sign-in prompt for unauthenticated users. Tested 100% (8/8 backend + all frontend). Files: `backend/routes/vinyl.py`, `frontend/src/components/VariantActions.js`, `frontend/src/pages/VinylVariantPage.js`.
- **Full Variant-First Search System** - DONE (2026-03-10). Replaced search modal with dedicated /search page. Variant-first scoring: variant match > color > album > artist. Discovery hub when empty: Trending Variants, Rare Variants, Most Wanted, Recently Added. Variant Quick Cards with album art, variant pill, collector count → link to `/vinyl/{artist}/{album}/{variant}`. Albums section with cover art scroll, Artists as clickable filter chips. Debounced real-time suggestions (<200ms). Infinite scroll pagination. Navbar search icon navigates to /search. Tested 100% (12/12 backend + all frontend). Files: `backend/routes/search.py`, `frontend/src/pages/SearchPage.js`, `frontend/src/App.js`, `frontend/src/components/Navbar.js`.
- **Unified Album Modal for ISO/Dreaming/Collection** - DONE (2026-03-10). Dreaming cards now open the same album modal as ISO items. Modal shows status-aware actions: WISHLIST→"Move to Actively Seeking", OPEN→"Mark as Found"/"Remove". Album art, variant, format, year, label, catno, condition all displayed from Discogs data. No automatic promotion — manual action only. File: `frontend/src/pages/ProfilePage.js`.
- **Global Variant Modal Refactor** - DONE (2026-03-10). Replaced ALL page-specific album modals with a single unified VariantModal rendered via React Context (`VariantModalProvider`). Clicking any album artwork across the entire app now opens the same rich variant pop-up showing: album art with variant label overlay, metadata badges (year, label, format, country), rarity score, market value, completion tracker, action buttons, and "View Full Page" link. Pages updated: ExplorePage, ExploreSeeAllPage, HivePage, ProfilePage (Collection/Dreaming/ISO/In Common tabs), SearchPage. Old modal code (trendingModal, albumModal, isoModal) fully removed. Tested 100% pass rate across all pages. Files: `frontend/src/context/VariantModalContext.js`, `frontend/src/components/VariantModal.js`, `frontend/src/App.js`, `frontend/src/pages/ExplorePage.js`, `frontend/src/pages/ExploreSeeAllPage.js`, `frontend/src/pages/HivePage.js`, `frontend/src/pages/ProfilePage.js`, `frontend/src/pages/SearchPage.js`.
- **Search Artist Deduplication** - DONE (2026-03-10). Fixed artist search results showing duplicates when records have multi-artist fields (e.g., "Taylor Swift, Post Malone" generating multiple Taylor Swift entries). Implementation: (1) Split multi-artist fields on commas, ampersands, "and", "with", "feat.", "ft.", "featuring". (2) Normalize artist names (lowercase, trim, strip trailing punctuation). (3) Deduplicate by canonical normalized name, preserving original casing from first occurrence. (4) Sort artists by: most records on HoneyGroove → search relevance score → alphabetical. Tested via API: searching "taylor" now returns one Taylor Swift entry + extracted collab artists (Post Malone, Brendon Urie). File: `backend/routes/search.py`.
- **BLOCK 13.1/14.2: Weekly Wax Email** - DONE (2026-03-10). Full CRM weekly email system with personalized stats, community trends, and social discovery. Schedule: Every Sunday at 12:00 PM ET (via `schedule_weekly_wax` async scheduler). Sections: (1) "Your Week on Wax" — weekly spins count, records added, taste matches, most recent spin (via `$lookup` join of posts→records). (2) "The Hive is Buzzing" — most spun record, rising variant, most wanted vinyl (weekly with all-time fallback). (3) "Collectors You Might Like" — top 3 unfollowed users ranked by records in common (excludes blocked/private). (4) Static sections: Getting Started, Trades, New Music Friday, Bug Alert. Safety: `sent_weekly_wax_at` timestamp prevents duplicates, batch sends in groups of 50, error logging without timestamp update on failure. Admin endpoints: `POST /api/admin/weekly-wax/send-test` (sends test email), `GET /api/admin/weekly-wax/preview` (previews data). Email sent via Resend with HoneyGroove branded template. File: `backend/routes/weekly_wax.py`.
- **Global Label Update: "Wishlist" → "Dream List"** - DONE (2026-03-10). Replaced all user-facing instances of "Wishlist", "Wantlist", "Dreaming" labels with "Dream List" across the entire platform.
- **Fix: Search Bar Visibility** - DONE (2026-03-10). Fixed search bar being hidden behind the fixed navbar by changing positioning from `sticky top-0 z-20` to `sticky top-[52px] md:top-[88px] z-30`. Made search input more prominent (larger height, thicker border, stronger focus ring). Removed redundant back button.
- **Fix: Variant Deduplication** - DONE (2026-03-10). Significantly improved variant grouping logic in `_dedup_key` and `_merge_variant_groups`. Added filler word stripping ("vinyl", "colored", "colour", "color", "standard", "edition", "version", etc.), weight descriptor removal (180g, 200 gram), format size removal, punctuation cleanup. Improved merge logic with 50% subset ratio guard to prevent "black" absorbing "gold splatter on black". Added Jaccard similarity (>= 0.6) for fuzzy matching. Result: 24 test variants correctly collapsed to 8 meaningful groups. Updated: CollectionPage (tab label, toggle button, DreamDebtHeader, subtitle, subtract animation, empty states), ProfilePage (tab label, stats, empty states, taste match text), ExplorePage + ExploreSeeAllPage (toast messages, empty states), AddRecordPage (title, toast), FAQPage (entire "Wantlist" section → "Dream List"), SettingsPage (delete account warning), MessagesPage (empty state), WaxReportPage (section titles), ListingDetailModal (button labels, toasts), VariantActions (button labels, toasts), Weekly Wax email (fallback text, section copy), and email templates (onboarding, wantlist match). Internal code (function names, status constants, API routes) left as-is per spec. Fixed bug: `setWishlistValue` reference in CollectionPage replaced with `setDreamlistValue`. All lint checks pass.
- **ISO Album Modal UX** - DONE (2026-03-10). Redesigned ISO items on ProfilePage from flat text list to visual card grid with album artwork (matching Collection tab layout). Added click-to-open modal with full album details: artwork, title, artist, year, variant/pressing, condition, tags, budget, status, and contextual actions (Mark Found, Remove, Chat, Discogs link). File: `frontend/src/pages/ProfilePage.js`.
- **Alt Text Standardization** - DONE (2026-03-10). Updated all AlbumArt alt tags across 18+ files to `{artist} {album} {variant} vinyl record` format. Zero empty AlbumArt alt tags remaining.
- **Full SEO & Metadata System** - DONE (2026-03-10). Comprehensive metadata implementation across all page types:
  - **Backend SSR endpoints** (`/api/ssr/*`): Pre-rendered HTML with full OG tags, Twitter Cards, JSON-LD, and vinyl-specific metadata for social preview bots. Covers: listings, records, profiles, collections, ISO lists, marketplace, Hive posts, and landing page.
  - **Bot detection middleware** (CRACO `onBeforeSetupMiddleware`): Intercepts requests from known bots (Twitterbot, facebookexternalhit, Discordbot, Slackbot, Googlebot, etc.) and proxies to backend SSR endpoints. Normal users get the React SPA.
  - **Client-side SEOHead component** (`react-helmet-async`): Dynamic meta tags on all page components — RecordDetailPage, ProfilePage, HivePage, ISOPage, CollectionPage, LandingPage, ListingDetailModal, AboutPage, FAQPage, TermsPage, PrivacyPage.
  - **Vinyl metadata**: vinyl:artist, vinyl:album, vinyl:variant, vinyl:color, vinyl:release_year, vinyl:label, vinyl:catalog_number, vinyl:format, vinyl:speed, vinyl:disc_count, vinyl:pressing_country, vinyl:media_condition, vinyl:sleeve_condition, vinyl:graded.
  - **Product metadata**: product:price:amount, product:price:currency, product:availability, product:condition.
  - **Trade metadata**: trade:available, trade:iso, trade:trade_type, trade:negotiable.
  - **Collector metadata**: collector:username, collector:collection_size, collector:iso_count.
  - **Post metadata**: post:type, post:artist, post:album, post:variant.
  - **Schema.org JSON-LD**: Product (listings), MusicRecording (records), ProfilePage (profiles), SocialMediaPosting (posts), CollectionPage (collections/marketplace), ItemList (ISOs), WebApplication (landing).
  - **Canonical URLs** on all pages. **Image alt attributes** fixed across all components.
  - Files: `backend/routes/seo.py`, `frontend/src/components/SEOHead.js`, `frontend/craco.config.js`, plus all page components.
- **Discogs Import 1,000 Record Limit Fix** - DONE (2026-03-10). Removed `$limit: 1000` from `get_my_records` aggregation pipeline, changed `.to_list(1000)` to `.to_list(None)` in both `get_my_records` and `get_user_records` endpoints. Collections of any size now fully supported. File: `backend/routes/collection.py`.

### P1
- ~~Weekly Wax Email~~ - DONE (2026-03-10). See completed section.

### P2
- Hauls Enhancement - dedicated page
- Refactor ISOPage.jsx - technical debt
- Grading Guide page (optional)

### Completed Recently
- **BLOCK 61: "Following" Feed Mode Toggle** - DONE (2026-03-10). Moved "Following" from content filter chips to a top-level segmented toggle (All | Following) with a sliding honey gradient indicator. Content type filters (Now Spinning, New Haul, ISO, etc.) now apply *within* the selected feed mode. Example: Following + ISO = ISO posts from people you follow. Files: `HivePage.js`.
- **BLOCK 60: Comprehensive Micro-Interactions — Honey/Bee Theme** - DONE (2026-03-10). Complete overhaul of App.css with luxurious, professional micro-interactions: (1) Like button: honey-colored with `honeyLike` bounce + `honeyBurst` ring animation. (2) Desktop nav: honey gradient underline on active/hover tabs (`nav-honey-link`). (3) Mobile bottom nav: active honey dot indicator, icon glow, tap scale. (4) Feed posts: staggered `feedSlideIn` entrance animation (5 posts staggered, rest grouped). (5) Notification badges: `badgePulse` glow animation. (6) Avatar: `avatar-honey-ring` glow on hover. (7) Album art: `album-art-hover` tilt+shadow on hover. (8) Modal entrance: honey-themed `modalHoneyIn` with blur deblur. (9) Global button press: `scale(0.96)` on active. (10) Dropdown menus: smooth `dropdownIn`. (11) Page transitions: smooth `pageIn` fade+slide. (12) Honey selection color, focus rings, scrollbar refinement, skeleton loader, tab transitions, follow success animation, count bump. (13) Logo hover: honey drop shadow glow. Files: `App.css`, `Navbar.js`, `HivePage.js`, `PostCards.js`.
- **BLOCK 58: UI Polish — Button Hierarchy, Price Spacing, Variant Labels, Micro-Interactions** - DONE (2026-03-10). (1) Buy Now is now the primary solid CTA, Make Offer is secondary outlined — improves conversion clarity. (2) Price section gets +8px top / +12px bottom breathing room for clear visual hierarchy. (3) Variant label strengthened: larger text (11px→semibold), more padding, bigger disc icon — easily scannable for variant collectors. Pressing info styled as metadata (e.g. "Sunshine Yellow · 2023 pressing"). (4) Album art corners rounded to 10px across all card types for cohesive feel. (5) Global micro-interactions: button press scale(0.96), card lift on hover, modal entrance animation, photo thumbnail zoom, heartPop keyframe. Files: `App.css`, `ListingDetailModal.js`, `PostCards.js`, `ISOPage.js`.
- **BLOCK 57: Editable Listings** - DONE (2026-03-10). Sellers can now edit all details of their ACTIVE listings via an "Edit Listing" button in the ListingDetailModal. Editable fields: price, shipping cost, description, condition, pressing/variant notes, listing type, photos (add/remove), insurance, international shipping. Backend PUT `/api/listings/{listing_id}` validates ownership, ACTIVE status, min 1 photo, $150 new-seller limit, and re-runs off-platform keyword detection. Once sold, listings are locked. Files: `honeypot.py`, `models.py (ListingUpdate)`, `ListingDetailModal.js`.
- **BLOCK 56: Variant Display on Hive Feed Cards + Make Friends Rename** - DONE (2026-03-10). Two changes: (1) Variant/color info now visible directly on Hive feed cards without clicking — `VariantTag` placed in text area below artist name for NOW_SPINNING, ADDED_TO_COLLECTION, DAILY_PROMPT, and listing cards. Removed overlay-on-album-art approach. Format pill fallback when no variant but non-default format. Discogs import now extracts `color_variant` from `formats[].text` field. (2) "Make Friends" feature: backend `/explore/suggested-collectors` updated to sort by shared records count (discogs_id overlap) instead of shared artists, and now excludes blocked users in addition to followed/hidden. Frontend labels already said "Make Friends". Files: `PostCards.js`, `collection.py`, `explore.py`.
- **BLOCK 55: @Mention Tagging in Hive Posts** - DONE (2026-03-09). Full @mention system: `MentionTextarea` component with autocomplete dropdown (fetches `/api/mention-search`), `MentionText` renders @mentions as clickable profile links. All 5 composer endpoints parse mentions and create MENTION notifications (capped at 10). Posts store `mentions` array. PostCards renders MentionText for all post types. CommentItem already had renderMentions. Files: `MentionTextarea.js`, `MentionText.js`, `ComposerBar.js`, `PostCards.js`, `hive.py`.
- **BLOCK 54: Daily Prompt Founder Label Bug Fix** - DONE (2026-03-09). The "Founder" badge was showing for ALL users in Daily Prompt slides because `founding_member` is `true` for all users (first 500 get it for fee purposes). Fixed by restricting the "Founder" label display to only `katieintheafterglow` username. File: `frontend/src/components/DailyPrompt.js`.
- **BLOCK 53: Privacy Settings + Follow Requests + DM Gating** - DONE (2026-03-09). Full 3-phase implementation:
  - **Phase 1 (Privacy + Follow Requests):** `is_private` toggle in Settings. Private profiles show locked content (Instagram-style) with mutual signals. Follow requests with accept/decline. "Request to Follow" / "Requested" button states. FollowRequestsBadge on own profile. All profile data endpoints gated.
  - **Phase 2 (DM Gating):** `dm_setting` (everyone/following/requests) in Settings. DM creation enforces setting. Message requests with pending status. Accept/decline in thread view. Separate "Requests" tab in Messages inbox.
  - **Phase 3 (Notifications):** Follow request notifications, follow request accepted notifications, DM request notifications.
  - DB Collections: `follow_requests {id, from_id, to_id, status, created_at}`, `dm_conversations` now has `status` field.
  - Files: `models.py`, `auth.py`, `explore.py`, `dms.py`, `SettingsPage.js`, `ProfilePage.js`, `FollowRequests.js`, `MessagesPage.js`.
- **BLOCK 52: Clickable Followers/Following with Records in Common** - DONE (2026-03-09). Backend: `/users/{username}/followers` and `/users/{username}/following` now return `records_in_common` count for each user (compared against viewer's collection via discogs_id intersection). Frontend: `UserRow` in `FollowList.js` shows "X records in common" with amber Disc icon when > 0. Files: `backend/routes/explore.py`, `frontend/src/components/FollowList.js`.
- **BLOCK 51: iOS Safari Scroll Freeze Fix** - DONE (2026-03-09). Three-layer fix for scroll freezing on iPhone Safari caused by Radix UI's `data-scroll-locked` attribute persisting after dialogs close: (1) Route-change cleanup in `ScrollToTop` — removes stale scroll locks on every navigation. (2) CSS: `-webkit-overflow-scrolling: touch` and `overscroll-behavior-y: none` on html/body. (3) Safety-net: `visibilitychange` listener + 3s periodic check that removes `data-scroll-locked` if no dialog/overlay is actually visible. Files: `App.js`, `index.css`.
- **BLOCK 50: User Blocking Feature** - DONE (2026-03-09). Full user blocking system: POST/DELETE/GET `/api/block/{username}`. When User A blocks User B, both become invisible to each other (profile 403, posts/comments/collections/spins/ISOs all filtered). Blocking auto-removes mutual follows. Feed, comments, and "My Kinda People" all filter blocked users. Block button on ProfilePage with confirmation dialog. "Profile Unavailable" screen for blocked profiles. Account deletion cleans up blocks. Files: `database.py`, `explore.py`, `auth.py`, `hive.py`, `collection.py`, `ProfilePage.js`.
- **BLOCK 49: Discogs Import Skipped Records Log** - DONE (2026-03-09). Backend now tracks each skipped record with `{title, artist, discogs_id, reason}` during Discogs import. Three reasons: `duplicate` (already in collection), `missing_data` (no discogs_id), `error` (exception with detail). Stored in `discogs_imports` collection and returned in `/api/discogs/import/progress` and `/api/discogs/import/summary`. Frontend: new `SkippedRecordsLog` collapsible component in Import Summary modal groups skipped records by reason with color-coded labels, icons, and Discogs links. Files: `backend/routes/collection.py`, `frontend/src/components/DiscogsImport.js`.
- **BLOCK 48: Global Back Button** - DONE (2026-03-09). Added a subtle fixed back button (ArrowLeft icon) to the AppLayout that appears on all pages except `/` (landing) and `/hive` (home) for authenticated users. Positioned just below the navbar (56px mobile / 94px desktop), 32px hit target, muted stone-400 color with hover effect. Uses `navigate(-1)` for standard browser-back behavior. data-testid="global-back-btn".
- **BLOCK 47: Final Terminology Cleanup** - DONE (2026-03-09). Renamed ALL remaining user-facing "Reality" → "Collection" and "Dream Value" → "Value of ISOs" across the entire app. Changes: Tab label "Reality (N)" → "Collection (N)", toggle pill "Reality" → "Collection Value", toggle pill "Dream Value" → "Value of ISOs", "Add to Reality" → "Add to Collection", "Confirm to Reality" → "Confirm to Collection", "Upgrade to Reality" → "Upgrade to Collection", "Bring to Reality" → "Bring to Collection", toast messages updated, backend acquire response message updated, ProfilePage "Dream Value" → "Value of ISOs", DreamDebtHeader label updated. Files: CollectionPage.js, AddRecordPage.js, ISOPage.js, ProfilePage.js, backend/routes/honeypot.py.
- **Variant Identity Pill**: Frosted glass variant pill overlay on album art across all card types (RecordCard, WishlistCard, ProfilePage collection/dreaming cards). Semi-transparent with blur, gold border, uppercase gold text, mobile-safe truncation.
- **BLOCK 46.1-46.4: Wishlist Value Rebrand + Modal Resync** - DONE (2026-03-09). Renamed all "Dream Value"/"Dream Debt" → "Wishlist Value". Collection header: "If only I had $X... (Wishlist Value)". Profile: "Wishlist Value: $X". Taste match: "You share $X in Wishlist Value." Modal: "Moving to Wishlist?" with "Add to Wishlist Value" CTA in Sunset Gold.
- **BLOCK 44.1-44.3: Taste Match Refinements + Dreaming Tab + Dream Value** - DONE (2026-03-09). New Dreaming tab on public profiles (6 tabs total) with "In Your Collection" badges and "Tell them about this pressing" DM CTA. Honey glow (box-shadow: 0 0 15px #FFD700) on common records, 30% opacity dimming on non-matching. Dream Value sub-headline on profile. New endpoints: GET /api/users/{username}/dreaming, GET /api/valuation/wishlist/{username}. ISO endpoint now excludes WISHLIST items.
- **BLOCK 43.1-43.4: Dream Value Rebrand + Stats Fix + Shared Dream Value** - DONE (2026-03-09). Renamed "Dream Debt" → "Dream Value" everywhere. Headline now reads "If only I had $X... (The Dream Value)" with lighter font weight subtitle. Modal CTA updated to Sunset Gold (#FFB300) with white text and glow. Mobile stats use justify-around for perfect alignment. Taste-match API now returns shared_dream_value showing how much Dream Value two users share.
- **BLOCK 41.1-41.3: Modal CTA + Stats + Taste Match Color Refactor** - DONE (2026-03-09). "Move to Dreaming" button now honey-gold gradient with chocolate brown text and floating glow. "Keep it" button has soft amber border. Following/Followers inline with | separator, 12px spacing to second row. Taste Match pill upgraded to matching honey-gold gradient.
- **BLOCK 39.1-39.3 & 40.1-40.3: Taste Match Engine + Profile Refactor** - DONE (2026-03-09). New backend endpoint GET /api/users/{username}/taste-match calculates compatibility score (shared artists 50%, shared records 30%, shared wishlists 20%). Profile page: stats refactored (Following/Followers top row, Records/Value second row); Taste Match pill below Follow button opens "Common Ground" overlay with Shared Collection, Shared Dreams, Perfect Swap sections; "Show Common Records" toggle filters collection tab; "In Your Collection" badge + "Start a Chat" CTA on ISO items; Founder tooltip on taste match pill; Est. Value links to /collection.
- **BLOCK 37.1-37.3: Collection Cleanse (Reverse Flow + Multi-Select)** - DONE (2026-03-09). Moving items from Reality to Dreaming/Hunt now shows themed confirmation dialogs ("Releasing to the clouds?" / "Back on the hunt?"). Moving to Hunt adds "Seeking Upgrade" tag. Added multi-select mode with "Dreamify" and "Huntify" bulk action buttons. New backend endpoints: POST /records/bulk-move-to-wishlist, POST /records/bulk-move-to-iso. Immediate optimistic value updates for Reality/Dream Debt counters.
- **BLOCK 34.1-34.3: Dream Debt Header & Reality Comparison** - DONE (2026-03-09). Dreaming tab now shows "If only I had $X..." with serif italic amber price and counting animation (easeOutCubic, 1400ms) on tab open. Empty fallback: "Your dreams are currently free. Go find some grails." Reality header now shows "The Gold Standard: $X worth of wax on your shelf." with gold gradient. "Bring to Reality" shows subtraction text and updates counter immediately. New endpoint: GET /api/valuation/record-value/{discogs_id}. Nav item renamed from "Reality" to "Collection".
- **BLOCK 31: "Upgrade to Reality" Modal** - DONE (2026-03-09). When clicking "Found It" on a Wantlist/Hunt item, a confirmation modal now appears with Media Condition dropdown, Sleeve Condition dropdown, and optional Price Paid input. New backend endpoint POST /api/iso/{id}/acquire stores condition/price data on the collection record. Confetti + toast + redirect on success. Old /convert-to-collection endpoint retained for backwards compatibility.
- **BLOCK 30: Context-Sensitive Add Record** - DONE (2026-03-09). Dynamic "Add to Reality" / "Add to Dreaming" button based on active tab. AddRecordPage reads ?mode param — Reality mode: POST /api/records (collection), Dreaming mode: POST /api/iso with WISHLIST status. Context-aware confirm buttons ("Confirm to Reality" / "Save to Dreams"), toasts, and headings. Color Variant selector on all add forms. ?tab=wishlist URL param auto-switches Collection to Dreaming tab.
- **BLOCK 29: Refreshed Reality Branding** - DONE (2026-03-09). Reality header → "The Gold Standard." with gold gradient text. Dreaming header → "In the Clouds." Sparkles icon on Reality tab. RecordCards glassmorphism. Reality Check toggle. "Bring to Reality" button.
- **BLOCK 28: "Reality" Refactor** - DONE (2026-03-09). Collection tabs renamed to "Reality" (owned) + "Dreaming" (wishlist). Desktop/mobile nav renamed from "Collection" → "Reality". Record card dropdown: "Move to Dreaming", "Put on The Hunt", "Remove Completely".
- **BLOCK 27.1: Collection Tabs Refactor** - DONE (2026-03-09). Collection page split into "The Hive" (Owned) + "Wishlist" (Dreaming) tabs. Wishlist tab: Dream Debt banner with total value, "In your dreams..." copy, Certified Delusional badge (>$5K), WishlistCards with ghost variant pills and "Ready to Buy?" promote button.
- **BLOCK 27.2: Honeypot Wantlist Refactor** - DONE (2026-03-09). ISO tab renamed to "Wantlist" with "The Hunt is On." header tagline. WISHLIST items moved to Collection's Wishlist tab.
- **BLOCK 27.3: Variant Pill Sync** - DONE (2026-03-09). VariantTag now has 4 modes: solid (owned), glass (album art overlay), ghost (dreaming, outlined), gold (hunting, gradient). Supports prefix prop ("Dreaming of", "Hunting"). New PUT /api/iso/{id}/promote endpoint changes WISHLIST → OPEN status.
- **"Dream Debt" Wishlist Calculator** - DONE (2026-03-09). GET /api/valuation/wishlist returns total median value of WISHLIST ISO items. Frontend shows editorial "Dream Debt" card at top of Wishlist section ("Total Value of Your Dreams: $X" / "You've got expensive taste, babe. Start playing the lottery."). Gold "Certified Delusional" badge when value > $5,000. WISHLIST filter added to ISO tab.
- **BLOCK 18.1: ISO "I Found It!" Hunt Flow** - DONE (2026-03-09). POST /api/iso/{id}/convert-to-collection migrates ISO → collection record (notes='Found via ISO'). Frontend: confetti cannon (honey colors), toast celebration, auto-redirect to /collection after 1.5s.
- **BLOCK 18.2: Collection Quick Actions** - DONE (2026-03-09). RecordCard dropdown now has 4 options: Log Spin, Move to Wishlist (→ ISO WISHLIST status), Put back on ISO (→ ISO OPEN status), Remove Completely. Backend: POST /api/records/{id}/move-to-wishlist and /move-to-iso.
- **BLOCK 19.1: Optimistic Likes** - DONE (2026-03-09). Like button now has instant visual feedback with rollback on failure + "Sticky situation—try again" toast. Larger mobile touch target (p-2, 20px hitSlop), active:scale-125 tap animation, touchAction:manipulation.
- **BLOCK 19.2: Color Variant Pill** - DONE (2026-03-09). VariantTag now has Disc icon + glassmorphism mode (backdrop-filter blur, dark transparent bg, white text) for album art overlays. Added to NowSpinning, DailyPrompt, and Listing cards. PostResponse now includes color_variant and pressing_notes.
- **BLOCK 19.3: Collector Notes Overlay** - DONE (2026-03-09). NowSpinningCard shows record.notes, ListingPostCard shows pressing_notes. Italic serif font, truncated to 60 chars.
- **CRITICAL: Feed Visibility Fix** - DONE (2026-03-09). GET /api/feed now returns ALL posts from all non-hidden users instead of only followed users + self. New users can see all posts in The Hive. Frontend "Following" filter updated to use actual following list fetched from /api/users/{username}/following.
- **Admin Remove User** - DONE (2026-03-09). Trash icon on each user row in Admin Panel > User Management. Confirmation modal with permanent deletion warning. Backend DELETE /api/admin/users/{user_id} removes user + all associated data (posts, comments, likes, followers, records, spins, ISOs, notifications, reports). Self-deletion prevented.
- **"New Feature" Tag System** - DONE (2026-03-09). Admin-only tag for Hive posts with muted green pill badge, subtle card emphasis (#f3faf5 background + shadow-md), feed filter support, and admin dropdown toggle via POST /api/posts/{post_id}/new-feature.
- **Notes/Bio Paste Fix** - DONE (2026-03-09). Fixed silent paste rejection in ComposerBar Notes (280), Settings Bio (160), Settings Setup (100). Changed conditional `if (len <= max)` pattern to `.slice(0, max)` truncation so paste always works.
- Tag Color Inconsistency - DONE (shared TagPill, ListingTypeBadge, PostTypeBadge components)
- Report Bug Screenshot Upload - DONE
- **Essentials Upsell Modal** - One-time checkout upsell showing Core Three products before Stripe redirect. "Yes, Show Me" opens /essentials in new tab + continues checkout. "No Thanks" continues checkout. localStorage-based one-time display
- Honey Shop Essentials (BLOCK 5.3) - DONE (static affiliate page at /essentials)
- Welcome to the Hive Dashboard (BLOCK 5.1) - DONE
- HONEY Order ID Branding (BLOCK 4.3) - DONE
- Navbar Report Button - DONE

- **Bug Fix: Dream List Value / Count Sync** - DONE (2026-03-10). Fixed inconsistency where "Value of Dream Records" showed $65 while Dream List count was 0. Root cause: frontend optimistic state updates leaked stale values without syncing the count, and no zero-count guard existed. Fixed at 3 layers: (1) Backend: forced $0 when `total_count == 0` in both `/api/valuation/dreamlist` and `/api/valuation/dreamlist/{username}`, (2) Frontend comparison toggle: hidden when `wishlistItems.length === 0`, (3) Frontend mutations (`handleConfirmCleanse`, `handleWishlistToISO`, `handleDeleteWishlistItem`): refetch from backend or force $0 when last item removed.
- **UX Fix: Search Page Fold** - DONE (2026-03-10). Tightened the search page layout so results appear immediately below the search bar without excessive whitespace. Changes: reduced search bar height from h-12 to h-11, reduced container padding (py-3→py-2 search bar, py-6→pt-3 results), tightened section margins (mb-8→mb-5), smaller variant card thumbnails (w-16→w-14), added pt-[52px]/md:pt-[88px] to account for fixed navbar. Sticky search bar remains pinned on scroll. File: SearchPage.js.
- **Feature: Collector-Intent Search Optimization** - DONE (2026-03-10). Overhauled search to be vinyl-collector-focused. Now searches both `records` AND `discogs_releases` collections. Added synonym expansion (rsd→record store day, limited→limited edition, etc.). New scoring prioritizes variant keywords (RSD, exclusive, limited) > artist+variant > artist+album. Extracts collector tags (RSD, Limited, Exclusive, Numbered, Signed, Tour) from notes field. Year matching gives +100 bonus. Frontend shows tags as dark pills and year in card titles. 105 results for "Taylor Swift RSD" with RSD variants ranked first. Files: search.py, SearchPage.js.
- **Feature: Variant Filter Chips** - DONE (2026-03-10). Added interactive filter buttons (RSD, Limited, Exclusive, Numbered, Signed, Tour) below the search bar. Clicking a chip filters variant results client-side by tag. Each chip uses its own color scheme matching the soft pill tag system. "Clear" button resets all filters. Shows "X of Y" count when filtering. Chips only appear when results have tags. File: SearchPage.js.
- **Refactor: ISOPage.js and ProfilePage.js** - DONE (2026-03-10). Extracted 4 card components (ISOCard, CommunityISOCard, ActiveTradeCard, ListingCard) from ISOPage into `components/honeypot/HoneypotCards.js`. Extracted EmptyState and WaxReportPin from ProfilePage into shared component files. ISOPage: 1273→1088 lines. ProfilePage: 1151→1106 lines.
- **Feature: Trade Auto-Completion Scenario 3** - DONE (2026-03-10). When neither user confirms delivery within 24h and no dispute is open, the trade automatically completes. Both mutual hold payments are released. Ratings left as null. Both users notified via in-app notification + email. Added `delivery_marked_at` field to trades when entering CONFIRMING status. Background task `auto_reverse_expired_holds()` now handles both Scenario 2 (partial confirmation) and Scenario 3 (no confirmation). Safeguards: skips DISPUTED trades, defers to Scenario 2 when one party confirmed. Files: trades.py, emails.py.
- **Feature: UPC on Variant Page** - DONE (2026-03-10). Added barcode/UPC extraction from Discogs release `identifiers` field in `database.py`. UPC displayed in the variant page metadata row as a monospace pill badge (e.g., "UPC: 1 90759 65231 2"). Hidden when no barcode exists. Invalidated cache to fetch fresh data with barcode field. Files: database.py, vinyl.py, VinylVariantPage.js.
- **Feature: Color + Year Filter Chips** - DONE (2026-03-10). Added dynamic Color and Year filter rows to the search page alongside existing tag filters. Colors extracted from variant names using keyword matching (Pink, Gold, Clear, etc.). Years extracted from release year field. Supports multi-select across all filter types (e.g., Pink + 2023 + RSD). Active chips show solid fill, "Clear all filters" button appears when any filter active. Count shows "X of Y" when filtering. Files: SearchPage.js.
- **Interactive Variant Tracker Checkboxes** - DONE (2026-03-10). Missing variants in the Variant Tracker now have clickable checkboxes. Clicking opens an AlertDialog confirmation ("Add to Collection?"). Confirming calls POST /api/records to add the variant, updates the progress bar and owned count in real-time, shows a toast notification, and triggers confetti on 100% completion. Unauthenticated users see "sign in to add" text. Handles 409 (already in collection) gracefully. File: VariantCompletion.js.

- **Bug Fix: Search Filter Clipping/Scrolling** - DONE (2026-03-10). Fixed two bugs in SearchPage filter bar: (1) Filter dropdown drawer was invisible because it was inside an `overflow-x-auto` container which clips absolutely-positioned children. Fixed by restructuring the layout to separate the dropdown from the scrollable chip row. (2) The `no-scrollbar` CSS class was referenced but never defined. Added `scrollbar-hide` utility class to App.css (`::-webkit-scrollbar { display: none }` + `scrollbar-width: none`). Active filter chips now scroll horizontally without clipping, and the filter drawer renders correctly. Files: SearchPage.js, App.css.
- **Critical Fix: Self-Titled Album & Discogs Fallback Search** - DONE (2026-03-10). Major search overhaul to fix collector-critical query failures. Changes: (1) **Discogs API fallback** added to `/search/variants` — searches Discogs when local results are sparse, when collector-intent keywords detected, when year in query, or for short queries. Results cached in `discogs_releases` for future searches. (2) **Multi-query strategy** — for artist+keyword queries, also searches Discogs for self-titled album variants (e.g., "Taylor Swift Taylor Swift vinyl"). (3) **Self-titled album detection** — exact match scoring boost when album title equals artist name. Extra +120 boost for "debut"/"self-titled" queries. (4) **Multi-word phrase detection** — "record store day", "test pressing" etc. treated as compound collector keywords, preventing false positives from individual word matches (e.g., "United Record Pressing"). (5) **Format field indexing** — RSD/Limited/Exclusive tags now extracted from Discogs format descriptions in addition to notes. (6) **Stop word filtering** — "by", "the", "a" etc. removed from search to prevent query pollution. (7) **Synonym expansion** — added "debut" → "self-titled"/"first album" mappings. All 6 required queries now surface the Taylor Swift self-titled RSD variant correctly. Files: search.py.

- **Feature: Year Range Dropdown Filter** - DONE (2026-03-10). Replaced cluttered year chips in search filter drawer with a compact Year range selector (From/To dropdowns). Supports single-year and range filtering. Active filter shows as a single compact chip (e.g., "2006–2018", "2006+", "≤2010"). Options are intelligently disabled to prevent invalid ranges (From can't exceed To). Available years extracted from current search results, sorted descending. Files: SearchPage.js.

- **Performance: Variant Tracker Optimization** - DONE (2026-03-10). Three optimizations: (1) **Parallelized Discogs API calls** — replaced sequential `await + sleep(0.3)` with `asyncio.gather` using a semaphore (5 concurrent). 20 uncached releases now fetch in ~1s instead of 6+s. (2) **Result-level caching** — completion data cached in `completion_cache` collection for 10 minutes. Subsequent page loads serve from cache (130ms vs 580ms cold). Cache auto-invalidated when user adds a record. (3) **Loading skeleton** — replaced `return null` with an animated pulse skeleton (progress bar, variant rows) so the tracker has a visual placeholder from the moment the page loads. Files: vinyl.py, collection.py, VariantCompletion.js.

### Recent Fixes (March 10, 2026)
- Added Dream List helper text on Collection page to clarify purpose vs Actively Seeking
- Fixed "Records in Common" count mismatch in Make Friends cards — was using `shared_records` (nonexistent field) instead of `common_count` from the `my-kinda-people` API
- Added proper singular/plural formatting for "record(s) in common" across Make Friends cards
- Removed duplicate "in common" display from Make Friends carousel
- **International Shipping Cost Feature:** Added `international_shipping_cost` field to listings. Sellers can now toggle "Offer International Shipping" which reveals a separate international shipping cost input. Listing detail displays "Shipping: $X (Domestic)" + "International Shipping: $Y" or "International Shipping: Not Available". Backend stores/returns the field with proper validation (disabling intl shipping clears the cost). Legacy data compatibility handled.
- Renamed `addToWantlist` → `addToSeekingList` globally across frontend
- Updated shipping field labels: "Domestic Shipping" + "International Shipping" with proper form layout (intl fields directly below domestic)
- **Hive Randomizer Feature:** Added Randomizer chip to ComposerBar. Picks a random record from user's owned collection (excludes dream list, ISOs, wantlist). Modal shows album art, title, artist, variant + caption field + 3 buttons: Post to Hive, Try Another, Cancel. Creates a distinct `RANDOMIZER` post type (separate from `NOW_SPINNING`). Backend endpoint `GET /api/collection/random` uses MongoDB $sample aggregation. New `POST /api/composer/randomizer` endpoint. Randomizer pill added to PostCards.js feed display. Shuffle animation on load.
- **Trade Flow End-to-End Enforcement:** Added EXPIRED trade status for shipping deadline misses with auto-expiry (releases mutual holds, notifies both users). Tracking validation: min 6 chars, carrier required. Trade step timeline UI with 5 steps (Accepted → Awaiting Shipment → In Transit → Awaiting Confirmation → Completed). Live countdowns for shipping and confirmation deadlines. Expired trade notice with reason display.
- **Sales Rating System:** 48-hour rating window after delivery for both buyer and seller. `POST /api/orders/{id}/rate` endpoint with 1-5 star validation + optional comment. Auto-complete orders when rating deadline expires (lazy check in `_enrich_transactions`). Notifications sent on delivery. Frontend `RatingSection` component with star input, countdown display, and completed/expired states.
- **Dream List Bug Fix:** Root cause: `GET /api/iso` used `{$ne: 'WISHLIST'}` while frontend filtered for `status === 'WISHLIST'` — always empty. Fix: Created dedicated `GET /api/iso/dreamlist` endpoint. "Value of Dream Records" metric now always shows (even $0).
- **Golden Hive ID Paid Verification:** $9.99 one-time payment via live Stripe. Flow: user pays → pending admin review → admin approves/rejects. Badge displays on profiles, Hive posts, and listings. Admin panel section for managing pending verifications. Endpoints: `POST /api/golden-hive/checkout`, `GET /api/golden-hive/status`, `GET /api/golden-hive/verify-payment`, `POST /api/admin/golden-hive/{id}/approve|reject`.

### Recent Fixes (March 10, 2026 - continued)
- **Golden Hive ID Modal UI Update:** Removed $9.99 price from the profile "Get Golden Hive ID" button. Clicking now opens a detailed modal explaining benefits (verified badge, priority visibility, increased trust, admin-verified identity) with the price revealed only at the bottom as "One-time verification — $9.99" and a "Get Verified Now" CTA. Files: ProfilePage.js.
- **FAQ Page Update:** Rewrote all FAQ content across 8 sections matching user-provided copy. Sections: Getting Started, The Hive, Collection, Dream List, The Honeypot Marketplace, Shipping/Ratings/Disputes, Trades, Safety and Trust.
- **48-Hour Window Standardization:** All trade-related windows updated from 24h to 48h across the entire platform. Backend auto-completion, hold confirmation deadlines, and dispute eligibility all use 48h. Frontend copy updated on FAQ, Terms, Landing, and Trades pages.
- **Full Dispute System for Sales:** Implemented buyer dispute flow within 48h of delivery. Structured reasons (5 options) + required photo evidence. Backend: `POST /api/orders/{id}/dispute` (buyer opens), `POST /api/orders/{id}/dispute/respond` (seller responds), admin endpoints for listing and resolving. Payout freezes on dispute open (`PAYOUT_ON_HOLD`). Admin approve → refund via Stripe, reject → payout proceeds. Abuse tracking via `dispute_losses` counter.
- **Trade Dispute Enhancement:** Updated trade disputes to use same structured reason dropdown (5 options) and require photo evidence. UI uses Select component instead of free-text. Reason labels displayed human-readable in dispute display.
- **Admin Sale Disputes Panel:** New "Sale Disputes" tab in admin panel. Shows open disputes with buyer evidence, seller response, listing details. Admin can approve (refund buyer) or reject (pay seller) with resolution notes.

### Recent Fixes (March 10, 2026 - continued 2)
- **Amber Glass & Brushed Gold Pill Refactor (BLOCK 69.1):** Standardized all variant and price badge overlays on album art to a consistent "Amber Glass & Brushed Gold" premium glassmorphism effect. Style: `rgba(255,215,0,0.2)` honey-tinted background, `blur(14px)` backdrop, `2px solid #DAA520` brushed gold border, soft outer glow with inner gold ring. Applied uniformly across: CollectionPage.js (collection + wishlist), ProfilePage.js (collection + dreaming + ISO tabs), PostCards.js (VariantTag component), VinylVariantPage.js, DailyPrompt.js.
- **Numbered Edition Tracking (BLOCK 71.1):** Added optional `edition_number` field to collection records and `preferred_number` field to Dream List/ISO items. Backend: `RecordCreate`, `RecordResponse`, `ISODirectCreate`, `ISOResponse` models updated. Frontend: "Edition Number (optional)" input on Add to Collection form, "Preferred Edition Number (optional)" on Dream List form. Display: "No. X" pill on collection cards, "Seeking No. X" pill on Dream List cards. Both pills use the Amber Glass & Brushed Gold glassmorphism style. Tested: 100% backend + frontend pass rate.
- **Treasury Header Refactor (BLOCK 72.1):** Replaced flat beige value bubbles with a premium "Treasury" dashboard on the Collection page. Features: Glassy Frosted Amber background (blur 16px), Brushed Gold Metal gradient border (DAA520), 24px serif font for currency values, gold honeycomb icon for Collection Value, gold cloud icon for Dream Records, count-up animation on page load, tab switching between Collection and Dream List. Removed redundant Collection Value Banner card. Tested: 100% pass rate.
- **Universal Price Visibility (BLOCK 73.1):** Made price badges appear on ALL Dream List cards. Items with Discogs median value show "$X"; items without show "---" placeholder. Backend enrichment added to `/users/{username}/dreaming` endpoint. Brushed Gold + Glassy Frosted styling applied to all badges. Tested: 100% pass rate.
- **Dual-Action Hunter Buttons (BLOCK 75.2):** Split single "Bring to Collection" button on Dream List cards into two sleek buttons: "Add to Collection" (solid honey gold, converts dream→collection via `/iso/{id}/convert-to-collection`) and "Actively Seeking" (glass with brushed gold border, promotes to ISO hunt via `/iso/{id}/promote`). Both equal height (h-7) with 8px gap. Tested: 100% pass rate.
- **Ultimate Glass Vault Refactor (BLOCK 78.1):** Replaced solid yellow Treasury header with high-translucency glass: `rgba(255,255,255,0.08)` background, `blur(30px) saturate(140%)`, `1px solid rgba(255,255,255,0.25)` crisp white edge, inner glow `inset 0 0 20px rgba(255,255,255,0.05)`. Value pills have zero background — only Brushed Gold borders and serif typography. Active tab uses subtle gold ring instead of solid background.
- **Global Mobile Header Fix (BLOCK 83.1):** Added `ml-10 md:ml-0` to Collection and Honeypot page title containers so the back arrow no longer overlaps the page title on mobile. Applied to both loading and loaded states.
- **True Frosted Glass Overhaul (BLOCK 86.1):** Treasury header refined: `rgba(255,255,255,0.05)` faint white background, `blur(40px) saturate(180%)` with `-webkit-` prefix, `1px solid rgba(255,255,255,0.2)` thin white edge, `box-shadow: 0 8px 32px 0 rgba(31,38,135,0.07)` soft outer glow. No yellow/gold border.
- **Mobile Header Clearance (BLOCK 86.2):** Increased mobile padding to `pt-20` (80px) and title offset to `ml-12` (48px) on both Collection and Honeypot pages. Glass Vault centered beneath title, no overlap.
- **Rarity UI Cleanup (BLOCK 87.1):** Removed standalone RarityPill from RecordDetailPage metadata tags. Only the full RarityCard (Owners/Wantlist/For Sale) remains below the fold.
- **Actual Glass Final Directive (BLOCK 87.2):** Treasury header: `rgba(255,255,255,0.01)` nearly 100% transparent, `blur(50px) saturate(200%)`, `1px solid rgba(255,255,255,0.15)` thin white edge. Made sticky (`sticky top-14`) so album covers blur underneath when scrolling.
- **Mobile Safe-Header (BLOCK 87.3):** Exact `pt-[64px]` and `ml-12` (48px) on Collection and Honeypot pages. Back arrow never touches title text.
- **True Glass Architecture (BLOCK 84.1):** Updated Treasury header to pure glass: `rgba(255,255,255,0.03)` near-zero background, `blur(40px) saturate(180%)` heavy blur, `1px solid rgba(255,255,255,0.2)` thin white highlight border. Removed solid gold border and inner glow shadow. Content behind the header now bleeds through the frosted glass.
- **True Frosted Glass Final Fix (BLOCK 88.1):** Root cause: 50px blur obliterated all color from album art into uniform cream; redundant opaque `bg-honey-cream` on AppLayout competed with body background. Fix: (1) Removed `bg-honey-cream` from AppLayout wrapper (body already provides `#FFF6E6`), (2) Set header background to white semi-transparent gradient that visually contrasts against warm cream background, (3) Enhanced border highlights and inner glow shadow for depth. Verified: colorful content scrolling behind header creates visible frosted color bleed.
- **Ambient Hive Glow + North Star Sticky (BLOCK 92.1 + 92.2):** Added permanent "Ambient Hive Glow" layer inside the glass header — `linear-gradient(90deg, #FFD700, #FF8C00, #8B4513)` with `filter: blur(80px); opacity: 0.4` creates a honey sunset trapped behind the glass so the refraction effect never dies. Sticky positioning moved to `top: 0` with `z-index: 1000` so album covers slide under it. Backdrop-filter set to `blur(50px) saturate(200%)` to handle the new gradient background.
- **Glass Architecture Final Spec (BLOCK 97.1):** Replaced container styling with exact spec: `background: rgba(255,255,255,0.01)` (1% opacity — nearly invisible clear lens), `backdrop-filter: blur(60px) saturate(210%)` (higher refraction makes honey glow pop), `border: 1px solid rgba(255,255,255,0.12)` (ultra-thin white edge). Removed box-shadow and thick borders. Refresh button remains bottom-centered.
- **Mobile Sticky Enforcement:** Added CSS `!important` declarations for the treasury header. Applied `overflow: visible` to AppLayout wrapper and `<main>`. Ensured `-webkit-overflow-scrolling: touch` on html/body (already present).
- **Global Anchor Fixed Override (BLOCK 108.1):** Moved Diamond Glass Vault from `position: sticky` to `position: fixed !important; top: 0; left: 0; width: 100%; z-index: 99999`. Added spacer div to preserve page layout since fixed removes element from flow. Removed `transform: translateZ(0)`. Verified no parent has `transform`, `perspective`, or `filter`.
- **Double Anchor Stack (BLOCK 109.1):** Nav bar at `top: 0; z-index: 100000`, Glass Vault at `top: 52px` mobile / `top: 88px` desktop with `z-index: 99999`. Spacer updated to `h-[200px] md:h-[240px]` (combined nav + vault height). Mobile bottom nav also bumped to `z-[100000]`.
- **Global Rarity Engine (BLOCK 85.1):** Replaced multi-factor rarity scoring with simple Discogs community.have count: Ultra Rare (<500), Rare (500-2500), Uncommon (2500-5000), Common (5001+). RarityPill shows "Global Rarity: [Tier]" with Discogs disc icon. RarityCard shows Owners/Wantlist/For Sale counts in gold glass container on RecordDetailPage.
- **Three-Step Hunting Pipeline (BLOCK 85.2):** Added PUT /api/iso/{id}/demote to revert OPEN ISOs back to WISHLIST. ISOCard shows gold lightning bolt (Zap) on Seeking items and "Back to Dreams" link. Full pipeline: Dreaming → Seeking → Collected with reversion support.
- **Now Spinning Vibe Assets (BLOCK 92.3):** Added interactive motion to Now Spinning feed cards: (1) Slowly rotating vinyl record SVG icon (top-left of album art, CSS `spin 4s` animation), (2) 3-bar animated gold equalizer (bottom-right, staggered `eqBar` keyframes). Also removed standalone floating `RarityPill` from SearchPage and ListingDetailModal — only the full Global Rarity Data Card (RarityCard) remains on RecordDetailPage. Cleaned up unused imports/state.
- **Variant-Specific Rarity Engine (BLOCK 94.1):** Refactored rarity to use variant-level Discogs release ID (not master). Updated tier thresholds: Ultra Rare (<500), Rare (500-2000), Uncommon (2001-5000), Common (5001+). Renamed UI labels to "Global Variant Rarity" in both RarityCard header and RarityPill. Backend `calculate_rarity()` in `routes/vinyl.py` updated, frontend `RarityBadge.js` relabeled.
- **Mobile Refresh & Nav Fix (BLOCK 94.2):** Moved the Refresh button from `absolute top-3 right-3` (overlapping values on mobile) to bottom-centered inside the glass vault. Restyled as a small circular brushed gold glass button (32px, `backdrop-filter`, gold gradient border) with only the sync icon — no text. Thumb-friendly pull-tab design.
- **Interactive Variant Registry (BLOCK 95.1):** Made every VariantRow in the Variant Tracker clickable — navigates to `/variant/{release_id}`. Added ChevronRight icon to far right, honey glass hover state (`hover:bg-amber-50/60`). Add-to-collection checkbox still works independently via `stopPropagation`.
- **Variant Detail Page + Precision Data Isolation (BLOCK 95.2):** New backend endpoint `GET /api/vinyl/release/{release_id}` returns variant-specific community.have, community.want, lowest_price from Discogs (NOT master album stats). New frontend page `VariantReleasePage.js` at route `/variant/:releaseId` displays variant-specific Owners, Wantlist, Lowest Price, Market Value, and "Global Variant Scarcity" rarity card. RarityCard now accepts `label` prop for customizable title.
- **Honeypot Market Sync (BLOCK 96.1):** The "For Sale" count in the Global Variant Scarcity card on Variant Detail Page now pulls from the internal Honeypot marketplace (not Discogs). Backend queries `listings` collection for active listings matching artist+album. Styled in Honey Gold (#FFD700). Shows "0 available in Honeypot" when none.
- **Buy Now Bridge (BLOCK 96.2):** The "For Sale" number and label in the RarityCard are now clickable (via `onForSaleClick` prop). On click, navigates to Honeypot search pre-filtered for the variant's artist + album. RarityCard supports `honeypotListings` prop to override Discogs "For Sale" with internal marketplace data.
- **External Link Purge (BLOCK 98.1):** Removed ALL `discogs.com` external links across 5 files: VariantReleasePage, VinylVariantPage, RecordDetailPage, VariantModal, DiscogsImport. Data (counts, release info) kept; links removed. Only remaining external connection is the Discogs Import/Sync in Settings (data-import only). Cleaned up unused imports (ExternalLink, Music2).
- **Honeypot Funnel (BLOCK 98.2):** Confirmed "For Sale" on Variant page pulls ONLY from internal Honeypot API. The only clickable market action navigates to internal Honeypot search. No external marketplace exit ramps remain.
- **Fuzzy Search Engine (BLOCK 99.1):** Implemented Fuse.js fuzzy search on the Honeypot marketplace. Searches across artist, album, pressing_notes, color_variant, and description fields with threshold 0.35 (1-2 typo tolerance). Exact matches sorted first by Fuse.js score. Filters both Shop and Trade tabs in real-time. Pre-fills from `?q=` URL param (linked from Variant Detail "Buy Now" bridge).
- **Glass Search Bar UI (BLOCK 99.2):** Added sticky glass vault header to the Honeypot page (same glass architecture as Collection: `backdrop-filter: blur(60px) saturate(210%)`, ambient hive glow). Search bar inside with `rgba(255,255,255,0.1)` background, `1px solid rgba(255,215,0,0.3)` gold edge, focus glow `boxShadow: 0 0 0 3px rgba(218,165,32,0.15)`. Real-time filtering as user types.
- **Crown Jewels Elite Carousel (BLOCK 103.1):** Replaced "Trending in Collections" with "Crown Jewels" section on Nectar/Explore. New backend endpoint `GET /api/explore/crown-jewels` aggregates internal records, fetches Discogs community.have for each variant, sorts by lowest global owners (rarest first), caches 24hrs. Frontend shows gold Crown icon, italic subtitle "The rarest records owned by Hive members", variant name with Crown badge, global owner count. See All page at `/nectar/crown-jewels`.
- **Crown Jewels Dual-Criteria Algorithm (BLOCK 105.2):** Upgraded Crown Jewels to use elite_score = 50% scarcity (low have) + 50% value (high median price). Backend fetches market data via `get_discogs_market_data()` for each variant. Returns `estimated_value`, `low_value`, `high_value` per record. Frontend displays **Honey Gold value badges** ($381, $177, etc.) on each cover art. Trending section renamed to "Trending in the Hive" with italic subtext "What Hive members have been spinning." Crown Jewels subtext updated to "The rarest and most valuable records owned by Hive members."
- **Boutique UI Refinements (BLOCK 85.3):** Dream List buttons at 32px height. "Add to Collection" solid #FFD700 gold. "Actively Seeking" transparent with 1.5px DAA520 border. "Back to Dreams" understated gold text link. Variant pills max-w-[70%] truncation protects price badge visibility.
- **Groove Gateway (BLOCK 115.1):** Amazon affiliate links now open a branded "Groove Gateway" transition modal. Shows spinning Honey Gold vinyl for 1.5s with "Securing your request..." text, then auto-opens Amazon in new tab. Handoff screen shows product preview, "You are being directed to our Amazon fulfillment partner. Your Groove status remains active." and "ACQUIRE VIA THE HONEY GROOVE PARTNER" gold button. All old "Honeypot Partner" / "Nectar" / "Secure External Checkout" branding replaced.
- **Groove Terminal (BLOCK 115.2):** VinylSupplyCo items open a "Groove Terminal" Diamond Glass iframe modal with sticky "The Honey Groove × VinylSupplyCo" header and gold X close button. If iframe is blocked after 5s, graceful fallback shows product preview + branded hand-off. Both modals support Escape key to close. Modal positioned below navbar to prevent click interception.

### Future / Backlog
- **P0:** User's secret search feature (details TBD — user has mentioned multiple times)
- Refactor `ISOPage.js` and `ProfilePage.js` to reduce complexity (break into smaller components)
- Extract repeated glassmorphism pill inline styles into a reusable component or CSS classes
- Safari-compatible loading animation
- "Pro" memberships / "Verified Seller" badge
- Buyer Protection features
- Re-enable Instagram sharing
- Make "New Music Friday" section of Weekly Wax dynamically editable by admin
- Break down GlobalSearch.js
- Replace star imports in backend
- Backend-powered search filters for scale
