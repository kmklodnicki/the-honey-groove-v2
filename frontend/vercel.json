{
  "buildCommand": "CI=false npm install --legacy-peer-deps && CI=false npx craco build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
