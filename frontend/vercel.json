{
  "installCommand": "echo skip",
  "buildCommand": "npm install --legacy-peer-deps && ./node_modules/.bin/craco build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
